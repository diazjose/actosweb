

<form method="post" action="{{ path('movimiento_delete', {'id': mov}) }}">

    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mov) }}">

    <div class="row mx-4 my-4">
      <div class="col">
        <h5>¿Seguro que desea eliminar el Movimiento?</h5>
      </div>
    </div>



    <div class="row">
      <div class="col">
        <div class="modal-footer">
            <button style="margin-left:15px;" class="btn btn-outline-danger btn-sm float-right" type="submit" >
            Si, eliminar
            </button>

            <button type="button" class="btn btn-default btn-sm float-right" data-dismiss="modal">
            No, cancelar
            </button>
        </div>
      </div>
    </div>

</form>


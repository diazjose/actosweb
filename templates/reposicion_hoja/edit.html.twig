{% extends 'base.html.twig' %}

{% block title %}Editar Persona - {{ parent() }}{% endblock %}

{% block body %}
    <br><br>
    <section class="content mx-2">
        <div class="container-fluid">
            <div class="row align-items-center mb-3 mb-xl-5">
                <div class="col-md mb-2 mb-md-0">
                    <h1 class="h3 mb-0">
                    Editar Hoja                    
                    </h1>
                </div>

                <div class="col-md ml-md-auto text-md-right">

                    <button class="btn btn-sm btn-outline-danger float-right" type="button" data-toggle="modal" data-target="#modalContent">
                    <i class="fas fa-trash-alt"></i> &nbsp; <span>Eliminar</span>
                    </button>

                    <a class="btn btn-sm btn-secondary float-right" style="margin-right:15px;" href="{{ path('hoja_show',{'id': reposicionHoja.hoja.id }) }}">
                    <i class="fas fa-arrow-left"></i> &nbsp; Volver a la Hoja
                    </a>
                </div>
            </div>

            {{ include('base/_flash.html.twig') }}

            <div class="row justify-content-center">
                <div class="col-md-12 align-items-center">
                    <div class="card card-success">
                        <div class="card-header">
                            <h3 class="card-title">Editar Hoja</h3>
                        </div>
                        <div class="card-body">
                            {{ include('reposicion_hoja/_form.html.twig',{'cant':reposicionHoja.cantidad}) }}
                        </div>
                    </div>
                </div>    
            </div>    
        </div>
    </section> 


{% endblock %}

{% block modal_content %}
  <div id="modalContent" class="modal fade" tabindex="-1" role="dialog">
    <div class="modal-dialog">
      <div class="modal-content">
        {{ include('personas/_delete_form.html.twig', {"persona" : reposicionHoja.id }) }}
      </div>
    </div>
  </div>
{% endblock modal_content %}
